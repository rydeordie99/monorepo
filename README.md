Personal monorepo of python packages, scripts and tools.

individual packages / scripts can be run from the root with `uv run python -m scripts.cloudflare` or from each subdirectory with `uv run main.py`
