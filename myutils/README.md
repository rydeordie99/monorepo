Shared functions that are used across all projects.

Requires the following environment variables:

EMAIL_TO_DEFAULT  
EMAIL_FROM_DEFAULT  
SMTP_SERVER  
SMTP_USER  
SMTP_PASS  
TELEGRAM_CHAT_ID  
TELEGRAM_API_KEY  
GOOGLE_VISION_API_KEY

To run tests use `uv run pytest`
